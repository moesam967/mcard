import 'package:flutter/material.dart';

class SocialMediaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('السوشال ميديا'),
      ),
      body: Center(
        child: Text('عرض خدمات السوشال ميديا المتاحة'),
      ),
    );
  }
}