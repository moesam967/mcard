import 'package:flutter/material.dart';

class UsersManagementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('إدارة المستخدمين'),
      ),
      body: Center(
        child: Text('صفحة إدارة المستخدمين'),
      ),
    );
  }
}