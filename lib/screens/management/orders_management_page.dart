import 'package:flutter/material.dart';

class OrdersManagementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('إدارة الطلبات'),
      ),
      body: Center(
        child: Text('صفحة إدارة الطلبات'),
      ),
    );
  }
}