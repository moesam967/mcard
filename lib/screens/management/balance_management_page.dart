import 'package:flutter/material.dart';

class BalanceManagementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('إدارة الرصيد'),
      ),
      body: Center(
        child: Text('صفحة إدارة الرصيد'),
      ),
    );
  }
}